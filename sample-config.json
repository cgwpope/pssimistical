{
    "tables": [{
        "columns": [{
            "name": "name",
            "type": "number",
            "index": true
        }],

        "name": "test"
    }],
    "inputs": [{
        "path": "/var/log/messages",
        "table": "syslog",
        "reader": "pss_tx_log"
    }]
}
